# Shell-Utils

This repository contains a set of shell utilities for convenience in scripting.

## The Structure

All modules/CLI are compiled into the same binary. Running `shell-utils --expand` will expand out the binary into each of its callables in the path of the shell-utils binary.
